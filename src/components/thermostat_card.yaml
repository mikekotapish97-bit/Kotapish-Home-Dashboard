# =============================================================================
# Component: Honeywell T6 Pro Thermostat (compact)
# Real entity: climate.t6_pro
#
# Deliberately just title + climate control, no history graph — this is
# one of three equal-width columns in pages/home.yaml's main row (camera |
# weather | thermostat), sized to fit a single 12.9" iPad screen with no
# scrolling. The full thermostat dial + 24h history graph lives on
# pages/climate.yaml, which has the vertical room for it.
# =============================================================================

type: vertical-stack
cards:
  - type: custom:mushroom-title-card
    title: Thermostat
    subtitle: Honeywell T6 Pro
    card_mod:
      style: |
        ha-card { background: transparent; box-shadow: none; border: none; backdrop-filter: none; padding: 4px 4px 0 4px; }
        .primary { font-family: var(--kotapish-font-family); font-weight: 700; font-size: 20px; }
        .secondary { font-family: var(--kotapish-font-family); color: var(--secondary-text-color); }

  - type: custom:mushroom-climate-card
    entity: climate.t6_pro
    show_temperature_control: true
    hvac_modes:
      - heat
      - cool
      - heat_cool
      - "off"
    collapsible_controls: false
    card_mod:
      style: |
        ha-card {
          --mush-control-height: 56px;
          padding: 18px 20px;
        }
        mushroom-shape-icon {
          --icon-color: var(--kotapish-orange);
          --shape-color: rgba(255, 159, 10, 0.14);
        }
